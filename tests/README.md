# Tests

Root-level integration, compatibility, fuzz, and transactional test crates live here.

Unit tests can still live beside their owning crates under `crates/`, but standalone
test packages belong here so the library tree stays focused.
