# Root simplification

Removed generated/root clutter:

- no `upstream/` root
- no `manifests/` root
- no `vendor/` root

The upstream support crate list lives at `domain folders under crates/.txt`, next to the synced upstream crates it controls.
